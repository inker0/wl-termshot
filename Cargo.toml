[package]
name = "wl-termshot"
description = "Wayland Terminal Screenshotter"
version = "0.2.0"
edition = "2024"

[dependencies]
chrono = "0.4.41"
cursive = { version = "0.21.1", features = ["toml"] }
dirs = "6.0.0"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
